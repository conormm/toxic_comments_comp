import numpy as np
import pandas as pd
import tensorflow as tf
import matplotlib.pyplot as plt

tf.nn.conv1d(value, filters, stride, padding)

plt.plot(x)
plt.show()

pd.DataFrame(np.random.randint(1, 10, size=(3, 10), dtype=np.int))

x = np.random.randn(10)

np.random.choice(x, 3, replace=False)
